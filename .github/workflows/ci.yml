name: CI

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  check-types:
    name: Type check
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-node-yarn
      - run: yarn check-types

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-node-yarn
      - run: yarn lint

  test:
    name: Unit tests (Vitest)
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-node-yarn
      - run: yarn test:run

  build:
    name: Build
    needs:
      - check-types
      - lint
      - test
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/setup-node-yarn
      - run: yarn build
